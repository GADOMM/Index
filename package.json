{
  "name": "perfumetr-importer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check scripts/tradedoubler-import.mjs",
    "test": "npm run check && node --test tests/*.test.mjs"
  }
}
